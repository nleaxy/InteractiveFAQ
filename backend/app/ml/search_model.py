# backend/app/ml/search_model.py
def find_best_question_idx(user_query: str, faq_questions: list) -> int:
    """
    Временная заглушка для семантического поиска.
    Принимает запрос пользователя и список вопросов из базы данных.
    Возвращает индекс самого похожего вопроса.
    """
    print(f"[ML LOG] Получен запрос от пользователя: '{user_query}'")
    
    # Пока модель не скачалась, притворяемся умными:
    # Если юзер ищет что-то про деньги, возвращаем первый попавшийся вопрос.
    # В реальности здесь будет работать sentence-transformers.
    
    return 0  # Всегда возвращает самый первый вопрос из базы данных
